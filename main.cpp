#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>


#include "Math.hpp"
#include "Geometry.hpp"

int main() {
    std::vector<Point> controls = {
        Point(0, 0),
        Point(0, 1),
        Point(1, 0),
    };

    return 0;
}
